# Scratch

## To be used for a Test for the Koopman Tensor

```bash
python -m koopman_tensor.test_tensor --env-id=LinearSystem-v0 --state-order=2 --action-order=2
python -m koopman_tensor.test_tensor --env-id=FluidFlow-v0 --state-order=2 --action-order=2
python -m koopman_tensor.test_tensor --env-id=Lorenz-v0 --state-order=2 --action-order=2
python -m koopman_tensor.test_tensor --env-id=DoubleWell-v0 --state-order=2 --action-order=2
```

Just let this run for the systems, and be done.

```
